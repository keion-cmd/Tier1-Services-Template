// Template booking URL — replace with the client's actual Calendly/booking provider URL before production.
export const BOOKING_URL = import.meta.env.VITE_BOOKING_URL || "[EXTERNAL_BOOKING_OR_CALENDLY_URL]";

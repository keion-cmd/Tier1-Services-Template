// Template Google Apps Script Web App URL for the lead-gen form intake — replace with the
// client's deployed Apps Script endpoint (bound to the intake Google Sheet) before production.
export const LEAD_FORM_ENDPOINT_URL = process.env.NEXT_PUBLIC_LEAD_FORM_ENDPOINT_URL || "https://hooks.example.com/[BUSINESS_LEADS_SLUG]";

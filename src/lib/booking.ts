// Template booking URL — replace with the client's actual Calendly/booking provider URL before production.
export const BOOKING_URL = process.env.NEXT_PUBLIC_BOOKING_URL || "https://cal.example.com/[BUSINESS_BOOKING_SLUG]";
